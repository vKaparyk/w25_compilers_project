/* Test file to test the associativity of operations.  
   Boolean operations are non associative and thus should raise errors */

void main(void) {
 
    if (3 < 4) {var = 3 < 4 < 5 < 6;}
    
    var = 3 + 4 - 5 + 6; // is valid;
    
    while (3 < 4 < 5) { a = 3;}

    callfunc(arg[0], arg[1]);
    return 5 > 4 >= 3;
}