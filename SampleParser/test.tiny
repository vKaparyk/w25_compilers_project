{ Sample program in Tiny language - compute factorial }

read x; { input an integer }
if x > 0 then { don't compute if x <= 0 }
  fact := -2 + 3;   { same as fact := 1; }

  repeat 
    fact := fact * $$;
    x := x - 1
  until x = 0;

  write fact & x { output factorial of x } 
{end}

